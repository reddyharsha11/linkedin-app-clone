import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent ensures that the environment is set up appropriately
// and points the app to your main App.tsx component.
registerRootComponent(App);